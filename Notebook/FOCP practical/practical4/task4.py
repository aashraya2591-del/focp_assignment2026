from math import *

# Now we can call sqrt, cos, radians, etc., without math.
root = sqrt(49)
val = cos(radians(45))

print("Square root of 49 is:", root)
print("Cosine of 45 degrees is:", val)
